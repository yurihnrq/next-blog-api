components:
  schemas:
    UserEmail:
      type: string
      description: The user's email.
      example: john@mail.com

    UserPassword:
      type: string
      description: The user's password.
      example: 12345678
